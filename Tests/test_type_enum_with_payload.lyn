# Test enum with payload arguments
enum Result {
    Ok(value),
    Err(message)
}

let success = Result.Ok(42)
let failure = Result.Err("something went wrong")
assert success != null, "enum payload ok failed"
assert failure != null, "enum payload err failed"
print("PASS: test_type_enum_with_payload")
