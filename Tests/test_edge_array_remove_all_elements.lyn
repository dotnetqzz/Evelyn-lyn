# Test emptying array by repeated popping
var arr = [10, 20, 30, 40, 50]
while arrayLen(arr) > 0:
    arrayPop(arr)

assert arrayLen(arr) == 0, "drain array failed"
print("PASS: test_edge_array_remove_all_elements")
